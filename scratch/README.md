# Scratch Space

Use this directory for local experiments, temporary generated output, and one-off inspection files
that do not belong in source control.

- keep only this README in git
- move durable smoke artifacts into `fixtures/`
- move durable documentation assets into `docs/`
