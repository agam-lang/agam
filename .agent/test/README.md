# Benchmark Workspace

1. This folder holds localized phase-work microbenchmarks, generated inspection artifacts, and temporary benchmark outputs tied to active optimization slices.
2. The organized benchmark workspace now lives under `benchmarks/` with suites, harnesses, CI helpers, results, and methodology docs.
3. Keep `.agent/tests/` for lightweight policy and documentation validation only.
